---
- name: "Install Zsh"
  ansible.builtin.apt:
    name: "zsh"
    state: "present"

- name: "Change {{ ansible_user }} user default terminal to Zsh"
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    shell: "/usr/bin/zsh"
