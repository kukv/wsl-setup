---
services:
  ansible-test:
    build:
      context: "./docker"
    container_name: "ansible-test"
    hostname: "ansible-test"
    volumes:
      - "./ansible:/ansible"
      - "./docker/ansible:/etc/ansible"
    privileged: true
    tty: true
    stdin_open: true
