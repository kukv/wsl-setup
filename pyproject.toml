[project]
name = "wsl-setup"
requires-python = ">=3.13"

dependencies = [
    "yamllint (>=1.37.0,<2.0.0)",
    "ansible (>=11.4.0,<12.0.0)",
    "ansible-lint (>=25.2.1,<26.0.0)"
]

[tool.poetry]
package-mode = false
